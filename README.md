# cloud-itonami-iso3166-deu

Open ISO 3166 Blueprint for **DEU** — **`:implemented`**.

Market-entry compliance actor (`marketentry`) with flagship HARD check
`local-rep-missing` and `ust-idnr-unverified`.

```
clojure -M:dev:test
clojure -M:dev:run
```

AGPL-3.0-or-later.
